[project]
name = "faker-outback-schools"
version = "0.1.0"
description = "Generate fake Australian outback school names"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "faker>=33.1.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.3.3",
]
